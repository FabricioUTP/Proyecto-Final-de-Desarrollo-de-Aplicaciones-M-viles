// src/theme/colors.js

const colors = {

  // ── Primarios ────────────────────────────────────────────
  primary:        '#4F46E5',   // Índigo — color principal (botones, headers, badges)
  primaryDark:    '#3730A3',   // Índigo oscuro — estados presionados, círculos decorativos
  primaryLight:   '#EEF2FF',   // Índigo muy claro — fondos de chips y badges activos

  // ── Secundarios ──────────────────────────────────────────
  secondary:      '#10B981',   // Esmeralda — tareas completadas, confirmaciones
  secondaryLight: '#D1FAE5',   // Esmeralda claro — fondo de badge "Completado"

  // ── Fondos ───────────────────────────────────────────────
  background:     '#F9FAFB',   // Gris muy claro — fondo general de pantallas
  surface:        '#FFFFFF',   // Blanco puro — cards, formularios, modales

  // ── Texto ────────────────────────────────────────────────
  textPrimary:    '#111827',   // Gris muy oscuro — títulos y texto principal
  textSecondary:  '#6B7280',   // Gris medio — subtítulos, placeholders, labels secundarios

  // ── Bordes ───────────────────────────────────────────────
  border:         '#E5E7EB',   // Gris borde — inputs, separadores, divisores

  // ── Prioridades ──────────────────────────────────────────
  priorityHigh:   '#EF4444',   // Rojo — prioridad alta
  priorityMed:    '#F59E0B',   // Ámbar — prioridad media
  priorityLow:    '#10B981',   // Esmeralda — prioridad baja

  // ── Estados y alertas ────────────────────────────────────
  danger:         '#EF4444',   // Rojo — errores, eliminar, alertas críticas
  dangerLight:    '#FFF5F5',   // Rojo muy claro — fondo de input con error
  warning:        '#F59E0B',   // Ámbar — advertencias
  warningLight:   '#FEF3C7',   // Ámbar claro — fondo de badge pendiente

  // ── Sombras ──────────────────────────────────────────────
  shadow:         '#000000',   // Negro puro — color base para sombras (con opacidad)

};

export { colors };